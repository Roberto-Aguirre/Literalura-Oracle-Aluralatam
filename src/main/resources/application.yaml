spring:
  application:
    name: literalura
  datasource:
    url: "jdbc:mysql://127.0.0.1:3307/literalura?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC"
    username: "root"
    password: "literaluraLatam"
  jpa:
    hibernate:
      ddl-auto: update
    # show-sql: true
    properties:
      hibernate:
        # format_sql: true
server:
  port: 8085